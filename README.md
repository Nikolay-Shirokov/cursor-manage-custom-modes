# Менеджер кастомных режимов Cursor

Инструмент для управления кастомными режимами (custom modes) в Cursor IDE.

## Возможности

- ✅ **Автоматическое определение** пути к базе данных Cursor
- 📋 **Просмотр** всех режимов (встроенных и кастомных)
- 📤 **Экспорт** режимов в JSON
- 📥 **Импорт** режимов из JSON
- ✏️ **Редактирование** режимов через JSON
- ➕ **Создание** новых режимов
- 🗑️ **Удаление** кастомных режимов
- 🛡️ **Защита** встроенных режимов от удаления

## Установка

Скрипт работает со стандартной библиотекой Python 3.x, дополнительных зависимостей не требуется.

## Использование

### Автоматический поиск базы данных

Скрипт автоматически найдет `state.vscdb` в стандартных местах:

**Windows:**
```
%APPDATA%\Cursor\User\globalStorage\state.vscdb
```

**macOS:**
```
~/Library/Application Support/Cursor/User/globalStorage/state.vscdb
```

**Linux:**
```
~/.config/Cursor/User/globalStorage/state.vscdb
```

### Запуск

#### Интерактивный режим

```bash
# Автоматический поиск базы данных
python manage_custom_modes.py

# Указание пути к базе данных явно
python manage_custom_modes.py --db /путь/к/state.vscdb

# Работа с базой данных в текущей директории
python manage_custom_modes.py --db state.vscdb
```

#### Программное использование

```python
from manage_custom_modes import CustomModesManager

# Автоматический поиск базы данных
manager = CustomModesManager()

# Или явное указание пути
manager = CustomModesManager("/путь/к/state.vscdb")

# Показать все режимы
manager.list_modes(show_builtin=True)

# Показать только кастомные режимы
manager.list_modes(show_builtin=False)

# Экспортировать режим
manager.export_mode("mode-id", "my_mode.json")

# Импортировать режим
manager.import_mode("my_mode.json")

# Удалить режим
manager.delete_mode("mode-id")

# Создать шаблон
manager.create_mode_template("template.json")
```

## Интерактивное меню

```
==================================================
Управление кастомными режимами Cursor
==================================================
1. Показать все режимы
2. Показать только кастомные режимы
3. Экспортировать режим
4. Импортировать режим
5. Удалить кастомный режим
6. Создать шаблон режима
0. Выход
```

## Формат режима

### Структура JSON

```json
{
  "id": "unique-mode-id",
  "name": "Название режима",
  "icon": "infinity",
  "description": "Описание режима (опционально)",
  "thinkingLevel": "none",
  "autoRun": false,
  "shouldAutoApplyIfNoEditTool": true,
  "enabledTools": [1, 18, 3, 6, 8, 5, 16, 7, 11, 15],
  "autoFix": true,
  "enabledMcpServers": [
    "server-1",
    "server-2"
  ],
  "customRulesForAI": "# Правила для AI\n\nВаши инструкции..."
}
```

### Обязательные поля

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | string | Уникальный идентификатор режима (UUID или строка) |
| `name` | string | Название режима, отображаемое в UI |
| `icon` | string | Иконка режима |
| `thinkingLevel` | string | Уровень "мышления": `none`, `low`, `medium`, `high` |
| `autoRun` | boolean | Автоматический запуск команд |
| `shouldAutoApplyIfNoEditTool` | boolean | Автоматическое применение изменений |
| `enabledTools` | array | Массив ID включенных инструментов |
| `autoFix` | boolean | Автоматическое исправление ошибок |
| `enabledMcpServers` | array | Массив названий включенных MCP серверов |

### Опциональные поля

| Поле | Тип | Описание |
|------|-----|----------|
| `description` | string | Описание режима |
| `actionId` | string | ID действия для привязки команды |
| `customRulesForAI` | string | Кастомные правила и промпты для AI |

### Доступные иконки

- `infinity` - бесконечность (для agent-режимов)
- `todos` - список задач (для plan-режимов)
- `cloudUpload` - облако (для background-режимов)
- `chat` - чат (для ask-режимов)
- `checklist` - чеклист (для spec-режимов)
- `bug` - жук (для debug-режимов)
- `hammer` - молоток
- `running` - бегущий человек
- `fileTwo` - файлы
- `edit` - редактирование

### Инструменты (enabledTools)

Основные ID инструментов:
- `1` - Read File
- `3` - List Files
- `5` - Search Files
- `6` - List Code Definition Names
- `7` - Execute Command
- `8` - Apply Diff
- `11` - Write to File
- `15` - Insert Content
- `16` - Delete File
- `18` - Ask Followup Question
- `41` - другие инструменты

## Примеры использования

### Создание нового режима

1. Создайте шаблон:
```bash
python manage_custom_modes.py
# Выберите пункт 6
```

2. Отредактируйте `mode_template.json`:
```json
{
  "id": "my-analyzer",
  "name": "Code Analyzer",
  "icon": "bug",
  "description": "Analyze code for issues",
  "thinkingLevel": "medium",
  "autoRun": false,
  "shouldAutoApplyIfNoEditTool": false,
  "enabledTools": [1, 3, 5, 6, 18],
  "autoFix": false,
  "enabledMcpServers": [],
  "customRulesForAI": "# Code Analyzer\n\nAnalyze code for:\n- Performance issues\n- Security vulnerabilities\n- Best practices violations"
}
```

3. Импортируйте режим:
```bash
python manage_custom_modes.py
# Выберите пункт 4
```

### Редактирование существующего режима

1. Экспортируйте режим:
```bash
python manage_custom_modes.py
# Выберите пункт 3, введите ID режима
```

2. Отредактируйте JSON файл

3. Импортируйте обратно с тем же ID:
```bash
python manage_custom_modes.py
# Выберите пункт 4
```

### Копирование режима

1. Экспортируйте исходный режим
2. Отредактируйте JSON, измените `id` и `name`
3. Импортируйте как новый режим

## Безопасность

### Резервное копирование

Перед внесением изменений рекомендуется создать резервную копию:

```bash
# Windows
copy "%APPDATA%\Cursor\User\globalStorage\state.vscdb" "%APPDATA%\Cursor\User\globalStorage\state.vscdb.backup"

# macOS/Linux
cp ~/Library/Application\ Support/Cursor/User/globalStorage/state.vscdb ~/Library/Application\ Support/Cursor/User/globalStorage/state.vscdb.backup
```

### Защита встроенных режимов

Следующие режимы защищены от удаления:
- `agent` - Agent режим
- `plan` - Plan режим
- `background` - Background режим
- `chat` - Ask режим
- `spec` - Spec режим
- `debug` - Debug режим

## Устранение проблем

### База данных не найдена

Если скрипт не может найти базу данных автоматически:

```bash
python manage_custom_modes.py --db "/полный/путь/к/state.vscdb"
```

### Ошибка импорта

Проверьте, что JSON файл содержит все обязательные поля:
- `id`, `name`, `icon`, `thinkingLevel`, `autoRun`, `shouldAutoApplyIfNoEditTool`, 
  `enabledTools`, `autoFix`, `enabledMcpServers`

### Режим не отображается в Cursor

1. Перезапустите Cursor после импорта
2. Проверьте, что режим корректно сохранен: экспортируйте его снова
3. Убедитесь, что ID режима уникален

## Дополнительные инструменты

### Извлечение всех данных

Используйте `extract_vscdb.py` для полного дампа базы данных:

```bash
python extract_vscdb.py
```

Результат будет сохранен в `state_vscdb_extract.json`.

## Лицензия

Этот инструмент предназначен для личного использования с Cursor IDE.

## Поддержка

При возникновении проблем:
1. Проверьте путь к базе данных
2. Убедитесь, что Cursor закрыт перед редактированием
3. Создайте резервную копию перед изменениями